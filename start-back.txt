# avvia docker;
sudo systemctl start docker
docker start postgres-userapi //se non parte in automatico

#controlla dati in db:
docker exec -it postgres-userapi psql -U user -d userdb -c "SELECT * FROM users_mac_;"

# Avvia l'app Spring Boot:
cd ~/workspace/user-api
mvn spring-boot:run

# verifica che app risponde:
curl http://localhost:8080/users
